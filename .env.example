# Optional. Enables real server-side photo analysis when configured.
ANTHROPIC_API_KEY=
ANTHROPIC_MODEL=claude-sonnet-4-6

# Optional. Enables model-backed copilot intent parsing before local fallback.
GEMINI_API_KEY=
GEMINI_MODEL=gemini-1.5-flash

# API server port used by npm run server.
PORT=5000
